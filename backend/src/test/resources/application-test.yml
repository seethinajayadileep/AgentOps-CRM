spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
    # Critical: Open Session in View is disabled in production
    # Tests verify EntityGraph fetching works without it
    open-in-view: false
  
  flyway:
    enabled: true
    locations: classpath:migration
    baseline-on-migrate: true

app:
  security:
    enabled: false
  showcase:
    external-actions-disabled: false
  auth:
    seed-demo-user: false

jwt:
  secret: test-jwt-secret-for-unit-tests-only
  expiration-ms: 3600000

# Test-specific logging
logging:
  level:
    com.agentopscrm: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
